// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract AccessVerifier {

    // Balance checking for the requirement

    // NFT Ownership verification

    // Temporary Pass Based Access to some servers/channels (kind of like special offer)

    // In case of channel having multi token requirement (like NFT and Token balance both need to be a certain amount rather than being either)
}